# NER Fleet Intelligence - backend dependencies
# Python 3.11 (see README.md for why 3.11 and not 3.14)

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Database. psycopg3 serves both the async application engine and the
# sync engine Alembic uses, from one connection URL.
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
geoalchemy2==0.16.0

# Authentication.
# argon2-cffi: Argon2id password hashing, per docs/SECURITY.md section 1.
# pyjwt[crypto]: token signing/verification. The crypto extra is included now
# so an RS256/JWKS verifier (Supabase Auth) can be added later without a
# dependency change - see app/auth/verifier.py.
argon2-cffi==23.1.0
pyjwt[crypto]==2.10.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx==0.28.1
