# Model registry

Every experimental hazard model, with its data, splits, metrics and status.
Status meanings: **EXPERIMENTAL** may appear in research/admin views and controls
nothing; **VALIDATED** may become one evidence input under the deterministic
policy; **REJECTED** is kept for the record. Nothing here is deployed unless its
row says VALIDATED, and today none does.

| Model | Version | Status | Training data | Features | Train | Validate | Where |
|---|---|---|---|---|---|---|---|
| landslide-day-logreg | 0.1 (13 Sep 2026) | **EXPERIMENTAL — not deployed** (held-out: temporal ROC-AUC 0.79, recall 0.70 @ FPR 0.19; geographic ROC-AUC 0.82, recall 0.69 @ FPR 0.21; rule baseline recall 0.30/0.23; calibration poor across regions) | NASA GLC NER slice (≤5 km accuracy) + ERA5-Land daily rain (Open-Meteo archive) + Copernicus DEM slope proxy; negatives = quiet days at the same sites | p1, p3, p7, p15, p30, max7, elevation, slope, month (sin/cos); lead-1 variant drops event-day rain | 2007–2014 (temporal) / lon < 93.5 E (geographic) | 2015–2017 / lon ≥ 93.5 E | `backend/scripts/hazard_validation/` → `docs/HAZARD_VALIDATION.md` |
| landslide-day overnight grid (13 Sep night, `scripts/hazard_validation/overnight.py`, sklearn research venv) | 0.2-interim | **EXPERIMENTAL — not deployed.** Phase A (NER slice, LOGREG / RF / HGB × 4 feature sets × 3 seeds, threshold chosen on TRAIN out-of-fold for recall ≥ 0.90): best by mean PR-AUC = plain logistic regression without month terms — temporal recall 0.85 @ FPR 0.35 (acc 0.72, ROC 0.82, PR 0.74 calibrated), geographic recall 0.95-0.97 @ FPR 0.57-0.77 (acc 0.38-0.53, ROC 0.82). Forests/boosting did NOT beat it (RF mean PR 0.56, HGB lower). Isotonic calibration: Brier 0.13 (geographic) / 0.19 (temporal); bins still over-confident on the temporal split. **98% accuracy: NOT reached on any held-out split.** Phase B (India-wide GLC, NER as the geographic holdout) was fetching ERA5 rain overnight — see `docs/HAZARD_EXPERIMENTS.md` / `.runtime/data/hazard/experiments/leaderboard.json` for the final board. | as 0.1 + India-wide GLC ≤5 km (phase B) | as 0.1 ± month, rain-only, lead-1 | temporal ≤2014 / geographic west-east (NER); all-India minus NER (phase B) | 2015-17 / east; NER (phase B) | `backend/scripts/hazard_validation/overnight.py` |
| landslide-day V2 (`scripts/hazard_validation/experiments_v2.py`, 14 Sep) | 0.3-research | **EXPERIMENTAL — not deployed; confound found.** (a) NER + NASA LHASA susceptibility class: temporal recall 0.85 / FPR 0.36 / acc 0.71 / precision 0.56 / PR 0.75 / Brier 0.17; geographic recall 0.97 / FPR 0.75 / acc 0.40 / precision 0.25 — susceptibility adds ≈0 over v0.2. (b) **Season-matched negatives** (same ±45 days-of-year, other years, k=6): temporal PR 0.49 / ROC 0.69, recall 0.86 @ FPR 0.73; geographic PR 0.33 / ROC 0.68, recall 0.91 @ FPR 0.79. Reading: most of v0.2's apparent skill was the monsoon-vs-dry contrast; within-season rain-day discrimination from ERA5-Land daily totals + slope is weak on this catalogue. This is the honest reason the model must not enter route decisions. Next levers (not done): hourly/IMERG rain intensity, ≥5 km location-accuracy filtering is already applied, Bhukosh field inventory (portal only, unreachable 14 Sep). | NER slice (random and season-matched negatives); India-wide set pending ERA5 fetch | v0.1 + `susc` (+ SoilGrids texture where fetched) | temporal ≤2014 / west | 2015–17 / east | `.runtime/data/hazard/experiments/V2_*.json`, `leaderboard_v2.json`, `docs/HAZARD_EXPERIMENTS.md` |
| landslide-day INDIA → NER (`overnight.py --expand-only`, `experiments_v2.py --datasets INDIA,INDIA_SM`, 14 Sep) | 0.4-research | **EXPERIMENTAL — REJECTED for production (FPR).** India-wide GLC ≤5 km (605 events, 3,025 rows, ERA5-Land daily rain + DEM slope) trained OUTSIDE the NER, held out on the NER (n=770, 154 events; no NER row in training). Rule baseline on the holdout: recall 0.25 @ FPR 0.03 (acc 0.83). Calibrated logistic regression: **recall 0.95, FPR 0.50, precision 0.32, F1 0.48, accuracy 0.59, PR-AUC 0.55, ROC-AUC 0.83, FNR 0.05, Brier 0.12**, confusion tp 147 / fp 308 / fn 7 / tn 308; temporal 2015–17 (n=888, 221 events): recall 0.90, FPR 0.38, precision 0.44, F1 0.59, acc 0.69, PR 0.58, ROC 0.81, Brier 0.14. Geographic generalization to the NER **holds** (ROC 0.83 ≈ in-region). **Season-matched negatives (INDIA_SM, k=6)**: NER holdout recall 0.93 @ FPR 0.72, PR 0.33, ROC 0.72; temporal recall 0.88 @ FPR 0.65, ROC 0.70 — the same seasonality confound as the NER slice. Decision per gate: generalizes → calibrate → **FPR not acceptable** (0.50 random / 0.72 season-matched) → stays EXPERIMENTAL, nothing deployed. 98%: not reached (best held-out accuracy 0.69). | India-wide GLC minus NER (random and season-matched negatives) | p1 p3 p7 p15 p30 max7 elevation slope (+susc ≈0) | 2007–14 / all-India minus NER | 2015–17 / NER bbox (lon ≥ 89.5, lat 21.5–29.5) | `.runtime/data/hazard/experiments/INDIA*_CALIBRATED.json`, `V2_*INDIA*`, `docs/HAZARD_EXPERIMENTS.md` |
| production rain rule (baseline) | route_risk RAIN_* thresholds (2.5 / 7.5 mm h⁻¹) | in production (deterministic, not a model) | — | hourly precipitation at corridor points | — | evaluated as the daily analogue (64.5 mm/day or 100 mm/3 d) in the same tables | `backend/app/domain/route_risk.py` |

Gate for VALIDATED: held-out set, no leakage, temporal AND geographic splits,
hazard-specific metrics with false-negative review, meaningful sample, and the
result must beat the baseline materially. Flood and weather hazard models:
none trained — flood evidence is GloFAS (provider model output) and weather is
Open-Meteo/MET Norway (provider model output); no ground-truth labels for
"road flooded/closed" exist in any source surveyed (see `docs/ROAD_MEMORY.md`).
