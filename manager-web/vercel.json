{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": [
    "CANONICAL manager host. See docs/DEPLOYMENT_ENV_MATRIX.md.",
    "netlify.toml beside this file is a documented alternative, not a second",
    "production target - do not deploy both and hand out two URLs.",
    "",
    "Set the Vercel project's Root Directory to `manager-web`. No descriptor",
    "can do that, and without it the build runs at the repository root and",
    "fails to find package.json.",
    "",
    "VITE_INTELLIGENCE_BASE_URL must be set as a Vercel Environment Variable.",
    "It CANNOT come from manager-web/.env.production - that file is gitignored",
    "(.gitignore `.env.*`) and never reaches the build."
  ],

  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci",

  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
